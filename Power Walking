
#include <bits/stdc++.h>
using namespace std;
#define int long long int


signed  main() {
   int t;
   cin>>t;
   while(t--){
      int n;
      cin>>n;
      map<int,int> mp;

      for(int i=0; i<n;i++){
          int x;
          cin>>x;
          mp[x]++;
      }

      vector<int> ans;

      for(int k=1;k<n;k++){
          if(k <= m.size()){
              ans.push_back(m.size());
          }

          else{
              int x= ans[ans.size() -1];
              ans.push_back(x+1);
          }
      }

      for(auto it: ans){
          cout<<it<<" ";
      }
   }
   
   return 0;
}
